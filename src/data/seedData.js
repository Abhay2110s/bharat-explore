import states from '../../public/data/states.json';
import trails from '../../public/data/trails.json';
import ranks from '../../public/data/ranks.json';

export const seedData = {
  states,
  trails,
  ranks,
};

export default seedData;
